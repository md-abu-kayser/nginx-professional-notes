# Nginx Best Practices 2026

- Use `nginx -t` before reload.
- Always set `proxy_set_header Host`.
- Enable gzip and proper cache headers.
- Keep configs modular using `include`.
- Monitor with Prometheus and Grafana.
