# Configuration File Syntax

Directives end with `;`. Contexts: `main`, `events`, `http`, `server`, `location`. Nested blocks with `{}`.

> 📘 Next: [Main Context & include Directive](02-main-context-and-include-directive.md)
